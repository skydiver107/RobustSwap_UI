import accountStyles from "./_account";
import baseStyles from "./_base";
import chartStyles from "./_chart";

const useStyles = {
    account: accountStyles,
    base: baseStyles,
    chart: chartStyles,
}

export default useStyles;